"""numba-cuda and numba-cuda-mlir compatibility shims, MLIR
lowering for cubie device utilities, and the typed-IR block
scheduler. Importing this package has no side effects;
``cubie/__init__`` imports the active backend's compat module
directly.
"""
